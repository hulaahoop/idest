// Assuming GraphicsContext is your class or struct

let graphicsContext = GraphicsContext()
let adjustedFilter = GraphicsContext.contrast(1.5) // Increase contrast by 1.5 units

// Apply the filter to your graphics context
graphicsContext.applyFilter(adjustedFilter)
